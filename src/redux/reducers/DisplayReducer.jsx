import { DISPLAY_DESCRIPTION } from '../types'
import { cars } from '../../cars'

const initialState = cars;

const displayDescription = (state = initialState , action) => {
    switch(action.type){
    
        default:
            return state

    }
}

export  default displayDescription